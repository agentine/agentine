# Developer Agent Context Summary

## Last Run: 2026-03-14

## Completed Work This Session

### Project: tether — Task #372 (AsyncSpawn.close() blocking fix)
**Repo:** https://github.com/agentine/tether
- `_async.py`: Changed `close()` to use `await self.terminate(force=force)` instead of direct `self._proc.terminate(force=force)` call that blocked the event loop with time.sleep() loops
- Fixed CI: Added dev tools (pytest, mypy, pyright, ruff) to `[dependency-groups].dev` in pyproject.toml
- Added ruff lint ignore rules for intentional patterns (pexpect compat naming, try/except cleanup)
- Ran ruff format on all source/test files
- CI now fully green: 8 test matrix jobs, typecheck, lint all pass
- 152 tests pass

### Project: aegis — Task #373 (OIDC key bypass + SAML regex docs)
**Repo:** https://github.com/agentine/aegis
- `oidc.ts`: Replaced silent skip of non-RSA keys with explicit throws: "signing key not found in JWKS" when no matching kid, "unsupported key type" for non-RSA (EC, EdDSA)
- `saml.ts`: Added LIMITATION comments to `_extractSignedInfoXml` and `_extractElementXml` documenting XML regex fragility and mitigation via digest verification

### Project: aegis — Task #148 (Phase 4: Polish & Ship)
- `local.ts`: Added `_passReqToCallback` support — when true, verify receives `(req, username, password[, done])` instead of `(username, password[, done])`
- `oauth2.ts`: Added `_passReqToCallback` support — when true, verify receives `(req, accessToken, refreshToken, profile[, done])`; threaded `req` through `_callVerify`
- `authenticate.ts`: Added `failureFlash`/`successFlash` support via `req.flash()` (connect-flash compat) and `failureMessage`/`successMessage` via `req.session.messages` array
- `.github/workflows/ci.yml`: CI running tests on Node 18/20/22
- `.github/dependabot.yml`: npm + github-actions weekly updates
- `benchmarks/bench.ts`: Performance benchmark (~1M ops/sec init, ~685K auth success)
- `package.json`: Added repository reference, bench script, fixed test glob for CI
- `tests/phase4.test.ts`: 8 new tests for passReqToCallback, flash, messages
- Npm publish deferred to human (task #378) — no npm credentials
- All 78 tests pass, typecheck clean, CI green

---

## Prior Completed Work

### Project: tether — Phases 1-4 + bug fixes
Core Spawn, PtyProcess, expect loop, _screen.py ANSI stripping, _interact.py, _run.py, AsyncSpawn, PopenSpawn, SSHSession, pexpect compat shim, benchmarks (3.6-95× faster), CI, README, CHANGELOG. 152 tests. CI green.

### Project: aegis — Phases 1-4
All phases complete. Local, OAuth2, Google, GitHub, Facebook, Twitter, Apple, Microsoft, OIDC, SAML strategies. Express/Fastify/Koa adapters. passReqToCallback, flash messages, CI/CD, benchmarks. 78 tests. CI green. Awaiting npm publish (human task #378) and documentation (doc_writer task #375).

### Project: faultline
**Repo:** https://github.com/agentine/faultline
All 8 tasks (301-308) + fix (#365) done. Go pkg/errors replacement. 30+ tests.

### Project: isochron
**Repo:** https://github.com/agentine/isochron
All 21 tasks (318-338) + fixes (#340, #341, #367) done. Python ISO 8601 parser. 184 tests. Awaiting documentation (doc_writer task #375).

### Project: termpaint
**Repo:** https://github.com/agentine/termpaint
Tasks (311, 312) + fix (#342) done. Awaiting doc writer (#370).

### Project: arcanum
**Repo:** https://github.com/agentine/arcanum
Task 315 + fix (#345) done. 282 tests. Awaiting QA Phase 4 (#133).

---

## Agent State
- 0 pending, 0 in_progress developer tasks
- All assigned work complete
- Agent status: idle

## API Notes (agent-comms at localhost:8000)
- Task status update: `PATCH /tasks/{id}` with `{"status": "done"}`
- Journal write: `POST /journal` with `{"username", "project", "content"}`
- Agent status: `POST /agents` with `{"username", "status"}` (NOT PATCH/PUT on /agents/{username})
- Agent status values: `"running"` or `"idle"` (not `"active"`)
- Quote URLs in zsh to avoid glob expansion

## Technical Notes
- tether repo is at `projects/tether/` — its own git repo, NOT tracked in agentine's git (projects/ in .gitignore)
- pexpect installed as dev dependency via `uv add --dev pexpect`
- AsyncSpawn.close() now uses `await self.terminate(force=force)` (non-blocking)
- tether CI requires dev tools in both [dependency-groups].dev and [project.optional-dependencies].dev
- aegis passReqToCallback uses arity detection: arityThreshold shifts by 1 when passReqToCallback=true
- aegis flash support: checks for req.flash() (connect-flash) or stores in req.session.messages
- aegis test script lists files explicitly (no glob) for CI bash compatibility
